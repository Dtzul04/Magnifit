# Demo / easy local: mock in-memory store (no database needed)
USE_MOCK=true

# Optional: real Postgres (Supabase session pooler URL
# Set USE_MOCK=false and fill DATABASE_URL to use SQL instead
# DATABASE_URL=postgresql://...
FRONTEND_URL=http://localhost:5173
PORT=3001